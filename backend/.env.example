# Local dev uses SQLite. For Postgres, change the datasource provider in
# prisma/schema.prisma to "postgresql" and set a Postgres connection string here.
DATABASE_URL="file:./dev.db"

# Port the API listens on
PORT=3001

# Comma-separated list of allowed browser origins (the Next.js frontend)
CORS_ORIGIN="http://localhost:3000"
